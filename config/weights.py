# config/weights.py

# 1. БАЗОВЕ НАВАНТАЖЕННЯ НА ПАЛЬЦІ (Static Base Effort)
# Це штраф за сам факт використання пальця, незалежно від руху.
FINGER_BASE_PENALTY = {
    "L_index": 1.0, "R_index": 1.0,
    "L_middle": 1.1, "R_middle": 1.1,
    "L_ring": 1.4, "R_ring": 1.4,
    "L_pinky": 1.8, "R_pinky": 1.8,
}

# 2. АСИМЕТРІЯ РУК (Hand Asymmetry Modifiers)
# Множники для конкретних зон, де ліва рука відчувається краще за праву.
ZONE_MODIFIERS = {
    # правило: лівою тягнутися до верхнього ряду мізинців/безіменних легше
    "L_top_outer": 0.9,  # Зменшуємо штраф на 10%
    "R_top_outer": 1.1,  # Збільшуємо штраф на 10% (правій важче)
}

# 3. МАТРИЦЯ ШТРАФІВ SFB (Same Finger Bigrams)
# Оцінюємо переміщення ОДНОГО пальця між двома клавішами.
# Формат: (Початковий Ряд, Кінцевий Ряд, Тип Руху)
SFB_PENALTIES = {
    # --- ВКАЗІВНИЙ ТА СЕРЕДНІЙ ПАЛЬЦІ (Сильні) ---
    "strong_fingers": {
        # правило 1: Вказівний з верхнього на домашній (в одному стовпчику) = ковзання.
        ("top", "home", "straight"): 2,

        # правило 2: Діагональ з домашнього на верхній (мінімум зручності).
        ("home", "top", "diagonal"): 6,

        # правило 3: З домашнього на нижній (для вказівного/середнього це норм).
        ("home", "bottom", "straight"): 3,
        ("home", "bottom", "diagonal"): 5,

        # Інші рухи сильних пальців
        ("home", "top", "straight"): 4,  # Просте тягнення вгору
        ("bottom", "home", "straight"): 4,  # Повернення знизу
    },

    # --- БЕЗІМЕННИЙ ТА МІЗИНЕЦЬ (Слабкі) ---
    "weak_fingers": {
        # правило 3: З домашнього на нижній для безіменного і мізинця - це погано.
        ("home", "bottom", "straight"): 7,
        ("home", "bottom", "diagonal"): 9,  # Діагональ ще гірше

        # Ковзання зверху вниз слабкими пальцями теж не таке зручне, як вказівним
        ("top", "home", "straight"): 4,

        # Тягнення слабких пальців вгору
        ("home", "top", "straight"): 6,
        ("home", "top", "diagonal"): 8,
    }
}

# 4. СПЕЦИФІЧНІ ЖОРСТКІ ШТРАФИ (Hard Constraints & SFS)
# Перескоки через ряд (Same Finger Skips) та найгірші діагоналі.
SEVERE_PENALTIES = {
    "sfs_penalty": 3,
    # правило 4: Діагональ правою з хом ров на верхній (якщо це специфічний дальній випадок)
    "R_home_to_top_far_diagonal": 13,

    # правило 4: З нижнього на верхній правою (SFS по діагоналі)
    "R_bottom_to_top_diagonal": 10,

    # Загальний SFS (перескок через домашній ряд одним пальцем) для будь-якої руки
    "SFS_straight": 10,  # Наприклад: нижній -> верхній в одному стовпці
    "SFS_diagonal": 12,  # Наприклад: нижній -> верхній по діагоналі
}

# 5. ШТРАФИ ЗА НЕДОЛІКИ ЧЕРГУВАННЯ ТА ПЕРЕКАТІВ (Trigrams & Rolls)
# Оцінка трьох літер підряд.
TRIGRAM_PENALTIES = {
    # Відсутність чергування: три натискання однією рукою підряд (One Hand Trigram - OHT)
    "OHT_inward": 2,  # Перекат до центру (мізинець -> вказівний). Нормально.
    "OHT_outward": 5,  # Перекат від центру (вказівний -> мізинець). Фізіологічно важче.
    "OHT_awkward": 8,  # Злам напрямку (наприклад: середній -> мізинець -> вказівний).

    # Пінбол (Pinballing): чергування рук на кожній літері (Ліва-Права-Ліва).
    # Часто це гірше, ніж хороший перекат однією рукою.
    "strict_alternation": 3,
}